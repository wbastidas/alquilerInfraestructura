# Reglas específicas del componente móvil SGAIE.
# MapLibre y NanoHTTPD ya incluyen sus propias reglas de consumidor (consumer
# proguard rules) dentro de sus AAR/JAR, por lo que no se requieren reglas
# adicionales en M1. Se deja este archivo como punto de extensión para M2+
# (p. ej. modelos de datos serializados para el GeoPackage/sync con ArcGIS).
